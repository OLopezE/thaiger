import styled from "styled-components";

const Transitioner = styled.section`
  min-height: 50svh;
  background: white;
  background: linear-gradient(0deg, #d4cdc0 50%, rgb(236,236,214) 100%);
`;

export default Transitioner;