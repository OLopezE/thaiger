import styled from "styled-components";

const PrototypeTitle = styled.h2`
    position: fixed;
    transform: rotate(270deg);
    color: #ffffff52;
    font-size: 7rem;
    bottom: 18rem;
    right: -19rem;
`;

export { PrototypeTitle };
